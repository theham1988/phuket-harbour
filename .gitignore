# ── OS & editor junk ──────────────────────────────────────
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
desktop.ini

# ── IDE / Cursor ──────────────────────────────────────────
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln

# ── Node (if you ever add a build step) ──────────────────
node_modules/
npm-debug.log*
yarn-error.log
.env
.env.local

# ── Large video files — use Git LFS instead ──────────────
# (these are tracked via .gitattributes with Git LFS)
# If you don't have LFS set up, uncomment the lines below
# to exclude them from git entirely and upload manually:
# assets/videos/*.mp4
# assets/videos/*.mov
# assets/videos/*.webm

# ── Build output (if added later) ────────────────────────
dist/
build/
